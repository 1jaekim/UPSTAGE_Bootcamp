# VCS / IDE
.git
.gitignore
.DS_Store

# Python
.venv
venv
**/__pycache__
**/*.py[cod]
.pytest_cache
*.egg-info

# Node (이미지 안에서 npm ci 로 새로 설치)
**/node_modules

# 프론트는 별도 이미지(frontend/Dockerfile)로 빌드
frontend

# 런타임/로컬 산출물 (compose 에서 볼륨으로 마운트)
*.db
backend/spokeeper.db
backend/data/uploaded_books

# 시크릿 (env_file 로 주입, 이미지에 굽지 않음)
.env
.env.*
*.bak
run-local.sh
